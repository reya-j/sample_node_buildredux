# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  - 0.11

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
 # pre_ci:
  #  - docker build -t=shippabledocker/hitchcock3_sample_nodetest:test1 .
   # - docker build -t=shippabledocker/hitchcock3_sample_nodetest:test2 .
   # - docker build -t=revathird/testimage .
  pre_ci_boot:
    image_name: shippabledocker/hitchcock3_sample_nodetest
    image_tag: latest
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'
  ci:
    - ps -eax
    - ls -al
  post_ci:
   # - docker build -t=quay.io/shippablequay/shiphitchcock1_sample_node_bb
    - docker tag -f shippabledocker/hitchcock3_sample_nodetest:latest shippabledocker/hitchcock3_sample_nodetest:test47
    - docker push shippabledocker/hitchcock3_sample_nodetest:test47
    - docker tag -f shippabledocker/hitchcock3_sample_nodetest:latest shippabledocker/hitchcock3_sample_nodetest:test48
    - docker push shippabledocker/hitchcock3_sample_nodetest:test48
integrations:
  hub:
    - integrationName: rc-docker
      type: docker
      
  #post_ci:
   # - docker push revathird/testimage
#git:
 #submodules: false


